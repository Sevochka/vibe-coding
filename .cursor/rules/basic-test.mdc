---
description: 
globs: 
alwaysApply: true
---
- Max width of quiz is 660px
- When asked to give "код отображения в посте", give this (project-name is name of the folder) - 
<iframe class="auto-height" height="668" id="special-quiz-{project-name}" src="https://vibe-coding-blush.vercel.app/quizes/{project-name}/" style="border: 0px;" width="100%"></iframe>

<script src="https://basic-tests-static.sports.ru/resize.js"></script>

- On each project generate resize.js file that you will include in index.html in the end (after all other scripts). Content of the file is - 
const resize = () => {
      const dataUTILS = {
        for: 'BASIC_TEST',
        action: 'resizeIframe',
        selector: `iframe[src*=\\/specials\\/${this.quizId}], iframe[src*=\\/special\\/basic-tests\\/${this.quizId}]`,
        sizes: {
          height: 2 * Math.floor(document.body.scrollHeight / 2) + 10,
        },
      };

      window?.top?.postMessage(JSON.stringify(dataUTILS), '*');
    }

const resizeObserver = new ResizeObserver(() => {
  resize();
});
resizeObserver.observe(document.body);
